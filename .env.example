# MongoDB (Atlas free tier or local)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/payment-platform?retryWrites=true&w=majority

# Redis (Upstash free tier or local)
REDIS_URL=redis://default:password@hostname:port

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Server
PORT=3000
NODE_ENV=development

# Base domain for subdomain extraction (localhost for dev, yourdomain.com for prod)
BASE_DOMAIN=localhost
